---
type: report
status: ready-for-review
tags: [base-v2, v0.8, workspace-scoping, release, report]
relatedTo: [workspace-scoping, base-v2]
---

# v0.8 Workspace Scoping — Release Readiness Report

**Built autonomously 2026-06-24 (Phases 45–50). Behavior-complete, installed (v0.8.0 on PATH), live-verified. Awaiting operator review + commit/tag/release.**

Source spec: `docs/workspace-scoping.md`. Per-phase commits were SKIPPED by directive (single milestone commit = your call). 3 design decisions logged to the graph (domain `development`, 2026-06-24).

## The problem (recap)
`base project list` + session-start `[Active Projects]` returned the flat union of every registered workspace from any CWD, and the paul ingest stamped foreign projects into the CWD's named graph (re-pollution). Open base in Extendly, get chris-ai-systems projects.

## What shipped (6 phases, each PLAN→APPLY→UNIFY closed)
| Phase | Delivered | Verified |
|---|---|---|
| 45 Scope Resolution | `src/scope.rs` — `current_workspace` + `home` (FS-free, longest-prefix component-wise) | unit 5/5 |
| 46 Read-Time Scoping | `base project list` defaults to current ws; `--all`/`--workspace`/`--unscoped`; pure `in_scope` | unit + **live** (6 vs 40; extendly empty) |
| 47 Write-Routing | paul ingest routes each project → `graph/ws/{home(#path)}` not CWD slug; idempotent | unit (home-routing + idempotency + unscoped fallback) |
| 48 Session-Start Scope | `[Active Projects]` scoped + `elsewhere: N across M` pointer; handoffs/memory already tier-scoped | unit (pure `render_working_set`) |
| 49 peerWorkspace | `ops:peerWorkspace` edge + `base project peer <slug> --workspace <name> [--remove]`; peer-aware list | unit + **live round-trip** (add→shows→remove→clean) |
| 50 Unscoped + Config | `[signal] scope = workspace\|global` knob; missing-`#path` backfill nudge | unit + **live** (knob flips union↔scoped; nudge shows 28) |

## Test status
- `cargo build --release` clean (0.8.0), no warnings.
- `cargo test --no-fail-fast`: all green **except two PRE-EXISTING known-deferred failures** (in STATE Deferred Issues, NOT caused by v0.8):
  - `store::tests::snapshot_rotates_to_keep_limit` — non-deterministic (mtime-ordering); passes in isolation.
  - `graph_injection_test::sync_gc_removes_stale_synced_rules_keeps_cli_rules` — test-isolation bug (reads real extension data).
- New tests added this milestone: scope `in_scope`/peer (11 total), paul ingest routing (2), active_awareness render (2).

## Live verification (installed v0.8.0, real chris-ai-systems graph)
- `base project list` → 6 scoped projects + nudge "28 project(s) have no #path (unscoped)…".
- `base project list --all` → 41 (full union).
- `base project list --workspace extendly` → "No projects in workspace 'extendly'." ← the spec's headline acceptance.
- `base project peer operator-kit-47 --workspace testpeer` → surfaces under `--workspace testpeer` → `--remove` → gone (graph left clean).
- `base config set signal.scope global` → list = 41 (union); reset to `workspace` → 6. Config left at default.
- `base doctor` → HEALTHY ✓.

## The diff (for review)
13 source files, +432/−81, + new `src/scope.rs`:
`src/scope.rs` (new), `cli.rs`, `config.rs`, `crud/project.rs`, `extract/paul_toml.rs`, `hook/session_start.rs`, `lib.rs`, `signal/active_awareness.rs`, `signal/mod.rs`, `tests/{crud_project,signal}_test.rs`, `Cargo.toml` (0.7.0→0.8.0), `Cargo.lock`.

## Your release steps (when satisfied)
```bash
cd ~/chris-ai-systems/apps/base-v2
git add -A                                  # or selectively; .paul/ is gitignored
git commit -m "feat(workspace-scoping): v0.8.0 — scope reads/writes to current workspace (Phases 45–50)"
git tag v0.8.0
# distribute as usual
```
Binary is ALREADY live at `~/.local/bin/base` (0.8.0) — the next session-start will home-route the graph (idempotent, corrective).

## Known items / deferrals (none block release)
- **Migration (`base graph reslot`, spec §E)** — deferred behind your planned full workspace reset. Write-routing keeps the graph clean going forward; old mis-tagged triples are tolerated by `#path`-derived read scoping. Build reslot only if the reset slips.
- **`[Active Tasks]` not workspace-scoped** — tasks carry no `#path` (need a task→project join). Follow-up.
- **Briefing peer-awareness** — `active_awareness` doesn't yet honor `peerWorkspace` (only `project list` does). Follow-up.
- **Graph hygiene advisory** (surfaced by doctor, unrelated to v0.8): workspace graph is 28 MB / 116k lines, 4 backups (74 MB) — consider `base graph compact` + prune `.bak`.
- Two pre-existing flaky/isolation test failures (above) — own deterministic-clock / test-isolation fixes.
